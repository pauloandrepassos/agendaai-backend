{
    "version": 2,
    "builds": [
        {
            "src": "dist/server.ts",
            "use": "@vercel/node",
            "config": {
                "includeFiles": [
                "**/*"
                ]
            }
        } 
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/server.ts",
			"methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
			"headers": {"Access-Control-Allow-Origin": "*"}
        }
    ]
}
  